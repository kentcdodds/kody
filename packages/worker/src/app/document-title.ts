/**
 * Back-compat title helpers. Prefer `#app/document-head.ts` for new work —
 * titles are one field of the shared document-head registry.
 */
export {
	DEFAULT_DOCUMENT_TITLE,
	NOT_FOUND_DOCUMENT_TITLE,
	resolveDocumentTitle,
} from '#app/document-head.ts'
