{
  "name": "@kody/home-connector",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --import tsx server/dev-server.ts",
    "start": "NODE_ENV=production node --import tsx --import ./src/sentry-init.ts index.ts",
    "start:mocks": "MOCKS=true ROKU_DISCOVERY_URL=http://roku.mock.local/discovery NODE_ENV=production node --import tsx --import ./src/sentry-init.ts index.ts",
    "test": "vitest run --root ../.. --project node-unit packages/home-connector/src"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.26.0",
    "@sentry/bun": "^10.45.0",
    "dotenv": "^17.3.1",
    "msw": "^2.12.14",
    "remix": "3.0.0-alpha.3",
    "tsx": "^4.21.0",
    "zod": "^4.3.6"
  }
}
